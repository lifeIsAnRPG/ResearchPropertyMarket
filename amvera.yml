meta:
  environment: python
  toolchain:
    name: pip
    version: 3.8
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn --workers=1 app:server
  persistenceMount: /data
  containerPort: 8050